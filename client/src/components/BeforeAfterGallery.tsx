import { beforeAfterCases } from "../data/content";
import { useLocale } from "../i18n/locale";
import { getTranslations } from "../i18n/translations";
export function BeforeAfterGallery() { const t = getTranslations(useLocale()); return <section className="section-band" id="work"><div className="section-shell"><div className="mb-8"><p className="text-sm font-bold text-teal">{t.gallery.eyebrow}</p><h2 className="mt-2 text-2xl font-bold tracking-[-0.02em] text-pine sm:text-3xl">{t.gallery.title}</h2></div><div className="grid gap-5 md:grid-cols-2">{beforeAfterCases.map((item) => <article className="overflow-hidden rounded-[16px] border border-ink/10 bg-white" key={item.before}><div className="grid gap-px bg-ink/10 min-[420px]:grid-cols-2"><figure className="relative aspect-[4/3] overflow-hidden bg-cream"><img src={item.before} alt={t.gallery.beforeAlt} loading="lazy" width={640} height={480} className="h-full w-full object-cover" /><figcaption className="absolute left-2 top-2 rounded-full border border-white/35 bg-white/90 px-3 py-1 text-xs font-semibold text-pine shadow-card backdrop-blur sm:left-3 sm:top-3">{t.gallery.before}</figcaption></figure><figure className="relative aspect-[4/3] overflow-hidden bg-cream"><img src={item.after} alt={t.gallery.afterAlt} loading="lazy" width={640} height={480} className="h-full w-full object-cover" /><figcaption className="absolute right-2 top-2 rounded-full border border-teal/25 bg-white/90 px-3 py-1 text-xs font-semibold text-teal shadow-card backdrop-blur sm:right-3 sm:top-3">{t.gallery.after}</figcaption></figure></div></article>)}</div></div></section>; }
