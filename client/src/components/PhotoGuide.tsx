import { Camera, CheckCircle2 } from "lucide-react";
import { useLocale } from "../i18n/locale";
import { getTranslations } from "../i18n/translations";
export function PhotoGuide() { const t = getTranslations(useLocale()); return <section className="section-band" id="photo-guide"><div className="section-shell grid gap-8 lg:grid-cols-[0.9fr_1.1fr] lg:items-center"><div><p className="text-sm font-bold text-teal">{t.photoGuide.eyebrow}</p><h2 className="mt-2 text-2xl font-bold leading-tight tracking-[-0.02em] text-pine sm:text-3xl">{t.photoGuide.title}</h2><p className="mt-4 max-w-xl text-sm leading-6 text-ink/70 sm:text-base">{t.photoGuide.intro}</p></div><div className="relative"><div className="dot-pattern-dark absolute -left-5 -top-5 hidden h-24 w-32 opacity-35 sm:block" aria-hidden="true" /><div className="relative grid gap-3 sm:grid-cols-3">{t.photoTips.map((tip, index) => <article className="rounded-[16px] border border-ink/10 bg-cream p-4" key={tip}><div className="mb-4 flex h-11 w-11 items-center justify-center rounded-full bg-white text-teal">{index === 0 ? <Camera size={20} /> : <CheckCircle2 size={20} />}</div><p className="text-xs font-semibold text-gold">{t.photoGuide.photo} {index + 1}</p><p className="mt-2 text-sm leading-6 text-ink/75">{tip}</p></article>)}</div></div></div></section>; }
