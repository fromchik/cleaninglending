import { useEffect } from "react";
import { CheckCircle2, X } from "lucide-react";
import { useLocale } from "../i18n/locale";
import { getTranslations } from "../i18n/translations";
export function SuccessMessage({ onClose }: { onClose: () => void }) { const t = getTranslations(useLocale()).success; useEffect(() => { function handleKeyDown(event: KeyboardEvent) { if (event.key === "Escape") onClose(); } document.addEventListener("keydown", handleKeyDown); return () => document.removeEventListener("keydown", handleKeyDown); }, [onClose]); return <div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/55 px-4 py-8" onClick={(event) => { if (event.target === event.currentTarget) onClose(); }}><div aria-labelledby="success-popup-title" aria-modal="true" className="relative w-full max-w-md rounded-[18px] border border-teal/25 bg-white p-6 text-pine shadow-2xl sm:p-7" role="dialog"><button aria-label={t.close} className="absolute right-3 top-3 rounded-full p-2 text-ink/45 transition hover:bg-ink/5 hover:text-ink" onClick={onClose} type="button"><X size={20} /></button><div className="flex items-start gap-3 pr-7"><CheckCircle2 className="mt-0.5 shrink-0 text-teal" size={28} /><div><p className="text-xl font-bold" id="success-popup-title">{t.title}</p><p className="mt-2 leading-6 text-ink/70">{t.text}</p></div></div><button className="mt-6 min-h-12 w-full rounded-xl bg-gold px-5 py-3 font-bold text-ink transition hover:brightness-95" onClick={onClose} type="button">{t.ok}</button></div></div>; }
