import { MapPin, MessageCircle, Phone } from "lucide-react";
import { useLocale } from "../i18n/locale";
import { getTranslations } from "../i18n/translations";
import { CtaButton } from "./CtaButton";
export function FaqContactSection({ onCtaClick }: { onCtaClick: () => void }) { const t = getTranslations(useLocale()); return <section className="section-band" id="faq"><div className="section-shell grid gap-5 lg:grid-cols-[1.15fr_0.85fr]"><div className="rounded-[16px] border border-ink/10 bg-white p-4 sm:rounded-[18px] sm:p-6"><p className="text-sm font-bold text-teal">{t.faq.eyebrow}</p><h2 className="mt-2 text-2xl font-bold tracking-[-0.02em] text-pine sm:text-3xl">{t.faq.title}</h2><div className="mt-4 divide-y divide-ink/10">{t.faqs.map((faq) => <details className="group py-4" key={faq.question}><summary className="cursor-pointer list-none font-semibold text-pine"><span className="flex items-center justify-between gap-4">{faq.question}<span className="text-lg leading-none text-teal transition group-open:rotate-45">+</span></span></summary><p className="mt-3 text-sm leading-6 text-ink/70">{faq.answer}</p></details>)}</div></div><div className="rounded-[16px] bg-pine p-4 text-white sm:rounded-[18px] sm:p-6"><p className="text-sm font-bold text-gold">{t.faq.contacts}</p><h2 className="mt-2 text-2xl font-bold leading-tight tracking-[-0.02em] sm:text-3xl">{t.faq.contactsTitle}</h2><div className="mt-5 space-y-3"><a className="flex items-center gap-3 rounded-[14px] bg-white/8 p-4 font-bold transition hover:bg-white/12" href={t.businessProfile.phoneHref}><Phone className="text-gold" size={20} />{t.businessProfile.phone}</a><a className="flex items-center gap-3 rounded-[14px] bg-white/8 p-4 font-bold transition hover:bg-white/12" href={t.businessProfile.telegramHref}><MessageCircle className="text-gold" size={20} />Telegram</a><div className="flex items-center gap-3 rounded-[14px] bg-white/8 p-4 font-bold"><MapPin className="text-gold" size={20} /><span className="min-w-0">{t.businessProfile.serviceArea}</span></div></div><CtaButton className="mt-5 min-h-12 w-full shadow-none" onClick={onCtaClick}>{t.faq.cta}</CtaButton></div></div></section>; }
